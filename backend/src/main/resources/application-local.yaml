# Local development configuration
# Start database: ./scripts/db-setup.sh start
# Run migrations: ./scripts/db-setup.sh migrate

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:postgres}
    username: ${DB_USER:phillipernst}
    password: ${DB_PASSWORD:NutriFit_post_5432}

# For local development, you can override JWT secret here
# In production, this MUST be set via environment variable
jwt:
  secret: ${JWT_SECRET:local-dev-secret-key-minimum-32-characters-long}
